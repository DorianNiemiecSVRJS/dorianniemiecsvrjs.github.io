---
// Derived from https://github.com/ferronweb/ferron ("2.x" branch), under MIT License
import { icon } from "@fortawesome/fontawesome-svg-core";
import "@fortawesome/fontawesome-svg-core/styles.css";

let classes = Astro.props.class?.split(" ");

if (Astro.props.fixedWidth) classes.unshift("fa-fw");

const iconHtml = icon(Astro.props.icon, {
  classes
}).html;
---

<Fragment set:html={iconHtml} />
